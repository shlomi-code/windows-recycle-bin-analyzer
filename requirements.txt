# Windows Recycle Bin Analyzer Dependencies
# This project uses only Python standard library modules
# No external dependencies required

# Required dependencies
# Python 3.6+ is recommended for best compatibility

# Optional dependencies (for better performance)
pywin32>=305; sys_platform == "win32"

# The following modules are used (all part of Python standard library):
# - os
# - sys
# - shutil
# - struct
# - datetime
# - pathlib
# - typing
# - argparse
# - csv
# - json
# - subprocess (no longer used - removed for security)

# Note: pywin32 is now required for Windows API access.
# The script will exit with an error if pywin32 is not available. 